<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.FTPServer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FTPServer" parent="Base.Theme.FTPServer"/>
    <style name="Theme.FTPServer.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="Theme.FTPServer.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="Theme.FTPServer.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

</resources>